
export interface SectionProps {
  id: string;
}

export interface CardItem {
  title: string;
  description: string;
  image?: string;
  link?: string;
  gradient?: string;
}

export interface NavItem {
  label: string;
  href: string;
}
